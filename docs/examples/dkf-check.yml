# Sample GitHub Action for repositories that hold a DKF workspace.
# Copy to .github/workflows/dkf-check.yml and adjust the `paths` filter and
# DKF_WORKSPACE to where your workspace lives.
name: DKF check
on:
  pull_request:
    paths: ["knowledge/**"]

permissions:
  contents: read
  pull-requests: write

jobs:
  check:
    runs-on: ubuntu-latest
    env:
      DKF_WORKSPACE: ${{ github.workspace }}/knowledge
      PARTICULARS_VERSION: v0.2.0
    steps:
      - uses: actions/checkout@v4

      - name: Install particulars
        run: |
          curl -sSL -o particulars.tgz \
            "https://github.com/nodelogicau/particulars-cli/releases/download/${PARTICULARS_VERSION}/particulars_${PARTICULARS_VERSION#v}_linux_amd64.tar.gz"
          # extract to a temp dir: a workspace at the repo root has a particulars/ directory
          tar -xzf particulars.tgz -C "${RUNNER_TEMP}" particulars
          sudo install "${RUNNER_TEMP}/particulars" /usr/local/bin/particulars
          particulars version

      - name: Validate workspace
        run: particulars validate

      - name: Index is current
        run: particulars index --check

      - name: Report open conflicts
        if: always()
        run: |
          {
            echo '### Knowledge awaiting synthesis'
            echo
            echo '```'
            particulars conflicts || true
            echo '```'
          } > conflicts.md
          cat conflicts.md >> "$GITHUB_STEP_SUMMARY"

      # Optional: draw the particulars this pull request touches, so a reviewer
      # sees the shape of what changed — which claim is the antithesis, whether
      # the new synthesis became the current belief — without cloning.
      #
      # A diagram discloses whatever it contains. In a PRIVATE repository the
      # comment reaches exactly the people who can already read the YAML in the
      # diff, so it discloses nothing new. In a public one, add
      # --scope organisation to the export, or leave this off. See
      # docs/visualise.md.
      #
      # - name: Draw what changed
      #   if: always() && github.event_name == 'pull_request'
      #   env:
      #     BASE_SHA: ${{ github.event.pull_request.base.sha }}
      #     MAX_SUBJECTS: "6"      # cap the comment; the excess is reported, never silently dropped
      #     MAX_CHARS: "8000"      # a large lineage is linked, not pasted
      #     DEPTH: "2"             # what changed and what it reconciles, not the whole chain
      #   run: |
      #     set -eu
      #     git fetch --depth=1 origin "${BASE_SHA}"
      #     subjects=$(git diff --name-only "${BASE_SHA}" HEAD -- 'claims/*.yaml' 'syntheses/*.yaml' \
      #       | xargs -r -I{} sh -c 'sed -n "s/^subject: //p" "$1"' _ {} | sort -u)
      #     if [ -z "${subjects}" ]; then
      #       echo "no claims or syntheses changed: nothing to draw"
      #       exit 0
      #     fi
      #     total=$(printf '%s\n' "${subjects}" | wc -l | tr -d ' ')
      #     {
      #       echo
      #       echo "### What changed"
      #       echo
      #       n=0
      #       printf '%s\n' "${subjects}" | while IFS= read -r s; do
      #         [ -n "${s}" ] || continue
      #         n=$((n + 1))
      #         if [ "${n}" -gt "${MAX_SUBJECTS}" ]; then
      #           echo "_${total} particulars changed; drawing the first ${MAX_SUBJECTS}._"
      #           break
      #         fi
      #         label=$(particulars particular resolve "${s}" --json | jq -r '.matches[0].label // "?"')
      #         diagram=$(particulars export --format mermaid --subject "${s}" --depth "${DEPTH}" || true)
      #         size=$(printf '%s' "${diagram}" | wc -c | tr -d ' ')
      #         echo "**${label}**"
      #         echo
      #         if [ "${size}" -gt "${MAX_CHARS}" ]; then
      #           nodes=$(particulars export --format mermaid --subject "${s}" --depth "${DEPTH}" --json | jq -r .nodes)
      #           echo "_${nodes} nodes (${size} characters) — too large for a comment. Draw it locally with particulars export --format mermaid._"
      #         else
      #           echo '```mermaid'
      #           printf '%s\n' "${diagram}"
      #           echo '```'
      #         fi
      #         echo
      #       done
      #     } >> conflicts.md

      - name: Comment on PR
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('conflicts.md', 'utf8');
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body,
            });
